// Entry point. physics.ts registers the block component and the
// event-driven neighbor checks; falling.ts receives the falling entity's
// landing reports. Both run when imported
import "./physics.js";
import "./falling.js";
