// How thick a patch of petals can get and what bone meal does to it.
export const PETALS_ID = "kai_templates:pink_petals";
export const PETALS_STATE = "kai_templates:petals";
// One block holds four flowers. Vanilla's own state counts 0 to 7 and only the
// first four draw anything, so this counts the flowers instead and stops there
export const MAX_PETALS = 4;
// Bone meal adds this many flowers to a patch that still has room
export const BONE_MEAL_PETALS = 1;
export const BONE_MEAL_ID = "minecraft:bone_meal";
