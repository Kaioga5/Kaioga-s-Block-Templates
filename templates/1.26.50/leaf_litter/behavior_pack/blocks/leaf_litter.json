{
  "format_version": "1.26.50",
  "minecraft:block": {
    "description": {
      // The ID for your custom block. Format must be namespace:block_name.
      "identifier": "kai_templates:leaf_litter",
      // Where the block appears in the creative inventory.
      "menu_category": {
        "category": "nature"
      },
      // Custom block states. The permutations below switch components on their values.
      "states": {
        "kai_templates:segments": {
          "values": {
            "min": 1,
            "max": 4
          }
        }
      },
      // Allows the block to use exposed vanilla block states.
      "traits": {
        // Sets the enabled state from the direction the player faces when placing.
        "minecraft:placement_direction": {
          "enabled_states": [
            "minecraft:cardinal_direction"
          ]
        }
      }
    },
    "components": {
      // Text shown for the block in the inventory and hotbar. Translated through the language files.
      "minecraft:display_name": "tile.kai_templates.leaf_litter.name",
      // Custom components are registered by the scripts in behavior_pack/scripts.
      "kai_templates:litter_spreading": {},
      // The vanilla or custom geometry used by the block.
      "minecraft:geometry": {
        "identifier": "geometry.kai_templates_leaf_litter",
        // Controls which bones are visible when the block is rendered.
        "bone_visibility": {
          "segment_1": "true",
          "segment_2": "q.block_state('kai_templates:segments') >= 2",
          "segment_3": "q.block_state('kai_templates:segments') >= 3",
          "segment_4": "q.block_state('kai_templates:segments') >= 4"
        }
      },
      // The texture or textures used by the block.
      "minecraft:material_instances": {
        "*": {
          "texture": "leaf_litter",
          // Controls how the block is rendered using vanilla render methods. Single-sided matters here: the quads have no thickness, and plain alpha_test draws their down faces from above as well, so every leaf shows twice, once mirrored, and the patch reads thicker than vanilla's.
          "render_method": "alpha_test_single_sided",
          // Whether neighbouring blocks darken this block's corners.
          "ambient_occlusion": 0,
          // Whether each face gets the vanilla directional shading.
          "face_dimming": false,
          // Tints the texture with a biome color, like grass or foliage.
          "tint_method": "dry_foliage"
        }
      },
      // The block's collision box.
      "minecraft:collision_box": false,
      // The block's selection box.
      "minecraft:selection_box": {
        "origin": [
          -8,
          0,
          -8
        ],
        "size": [
          16,
          1,
          16
        ]
      },
      // Faces and blocks the block may be placed against. When the rule stops holding, the block pops off.
      "minecraft:placement_filter": {
        "conditions": [
          {
            "allowed_faces": [
              "up"
            ]
          }
        ]
      },
      // Break time in seconds, with faster speeds for specific tools.
      "minecraft:destructible_by_mining": {
        "seconds_to_destroy": 0
      },
      // Resistance to explosions. Bedrock stores five times the blast resistance the wiki lists for a block, so the listed number goes in multiplied by five.
      "minecraft:destructible_by_explosion": {
        "explosion_resistance": 0
      },
      // The engine\'s own empty loot table, so the block drops nothing. No file of that name has to ship in this pack.
      "minecraft:loot": "loot_tables/empty.json",
      // How easily fire catches on and destroys the block.
      "minecraft:flammable": {
        "catch_chance_modifier": 60,
        "destroy_chance_modifier": 100
      },
      // How much light the block stops. 0 lets light through, 15 blocks it fully.
      "minecraft:light_dampening": 0,
      // How the block behaves when it detects liquids, such as waterlogging or being washed away.
      "minecraft:liquid_detection": {
        "detection_rules": [
          {
            "liquid_type": "water",
            "can_contain_liquid": true,
            "on_liquid_touches": "no_reaction"
          }
        ]
      },
      // Lets a player place another block into this block's space, like tall grass.
      "minecraft:replaceable": {},
      // How pistons can move the block.
      "minecraft:movable": {
        "movement_type": "popped"
      },
      // Texture used for the break particles.
      "minecraft:destruction_particles": {
        "texture": "leaf_litter",
        "tint_method": "dry_foliage"
      },
      // Vanilla sound set used for placing, breaking and stepping on the block. blocks.json would override this, so the template does not ship one.
      "minecraft:sound": {
        "sound": "leaf_litter"
      },
      // Color the block shows on maps.
      "minecraft:map_color": {
        "color": "#8F7748",
        "tint_method": "dry_foliage"
      },
      // Whether blocks with connection behavior, such as fences and panes, can connect to this block.
      "minecraft:connection_rule": {
        "accepts_connections_from": "none"
      }
    },
    // Component overrides that apply while their Molang condition is true.
    "permutations": [
      {
        "condition": "q.block_state('minecraft:cardinal_direction') == 'south'",
        "components": {
          // Rotates, scales or offsets the geometry and boxes for this permutation.
          "minecraft:transformation": {
            "rotation": [
              0,
              180,
              0
            ]
          }
        }
      },
      {
        "condition": "q.block_state('minecraft:cardinal_direction') == 'west'",
        "components": {
          "minecraft:transformation": {
            "rotation": [
              0,
              90,
              0
            ]
          }
        }
      },
      {
        "condition": "q.block_state('minecraft:cardinal_direction') == 'east'",
        "components": {
          "minecraft:transformation": {
            "rotation": [
              0,
              -90,
              0
            ]
          }
        }
      }
    ]
  }
}
